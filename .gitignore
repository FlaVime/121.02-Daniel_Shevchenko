# no .a files
*.class
*.jar
# but do track junit.jar, even though you're ignoring.jar files above 
!junit.jar
# ignore all files in the build/ directory 
build/
# ignore all .txt files in the doc/directory 
doc/**/*.txt
